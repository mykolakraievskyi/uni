package Enums;

public enum PointType {
    Start, // starting point
    Point, // tourist valuable stop (i.e. went to the waterfall)
    Stop,  // non tourist valuable stop (i.e. stop for fuel, toilet)
    End    // end of the trip (destination)
}
